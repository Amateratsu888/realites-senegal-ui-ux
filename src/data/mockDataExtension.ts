import { Property } from '../types';

// Additional properties 46-93 to extend the main mockData file
// Note: IDs start at 46 to avoid duplicates with main mockData (IDs 1-45)
export const additionalProperties: Property[] = [];
